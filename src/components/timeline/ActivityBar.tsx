// ActivityBar removed — keeping file to avoid import errors elsewhere
// Activity stats and recent summaries may be re-added later
export function ActivityBar(_props: { sessionId: string; children?: React.ReactNode }) {
  return null
}
